spring:
  application:
    name: Max-Griffin-Project

  datasource:
    url: jdbc:postgresql://localhost:5432/max_griffin_project
    username: local
    password: local_user123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  security:
    user:
      name: TestUser
      password: SantaClause32!
    filter:
      order: 10
